# AgroScan

Real-time, crop-agnostic plant disease detection app.

## Setup
1. Run `flutter pub get`
2. Run `flutter packages pub run build_runner build`
3. Setup the FastAPI backend in the `backend/` folder.
